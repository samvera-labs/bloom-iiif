import { styled } from "stitches";

const Anchor = styled("a", {
  color: "black",
  textDecoration: "none",
});

const ItemStyled = styled("div", {
  width: "calc(100% / 4)",
  zIndex: "0",
});

export { Anchor, ItemStyled };
